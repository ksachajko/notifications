services:
  php:
    build:
      context: ./docker
      dockerfile: Dockerfile
    volumes:
      - .:/app
    working_dir: /app
    extra_hosts:
      - "host.docker.internal:host-gateway"
    dns:
      - 8.8.8.8
      - 1.1.1.1
